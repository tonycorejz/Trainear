package com.example.trainear.viewModels;

import android.graphics.drawable.Drawable
import android.widget.ImageView
import androidx.lifecycle.LiveData;
import androidx.lifecycle.MutableLiveData;
import androidx.lifecycle.ViewModel;
import androidx.navigation.fragment.navArgs
import com.example.trainear.R



class UprTemplateViewModel : ViewModel() {

    /*private val nav_direction by navArgs<UprTemplateFragmentArgs>()

    private val _text = MutableLiveData<String>().apply {
        if (exercises.isNotEmpty()) {
            //for (exercise in exercises) {
            view.findViewById<ImageView>(R.id.uprazneniya_main_img).setImageDrawable(Drawable.createFromPath("R.drawable."+exercises[0].exercise.image.toString()))
            //}
        }
    }*/

    /*init{

    }

    public fun setData(exercises:List<ExerciseWithDirection>, view:View) {
        if (exercises.isNotEmpty()) {
            //for (exercise in exercises) {
            view.findViewById<ImageView>(R.id.uprazneniya_main_img).setImageDrawable(Drawable.createFromPath("R.drawable."+exercises[0].exercise.image.toString()))
            //}
        }
    }*/

}
